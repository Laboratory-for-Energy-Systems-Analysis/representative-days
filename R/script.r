# The script file for this package is in folder "inst" (and not in
# folder "R" as you migh expect) under the same file name.
# This is because files in folder "inst" are not evaluated during
# package-creation (build()), which gives usually errors if evaluated
# (paths not found etc.)
